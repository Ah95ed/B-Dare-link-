# خطة اختبار التحسينات - Wonder Link Game

## 🎯 أهداف الاختبار

1. ✅ التحقق من عدم وجود تراجع في الوظائف
2. ✅ قياس تحسن الأداء
3. ✅ التأكد من استقرار التطبيق تحت الحمل
4. ✅ التحقق من صحة معالجة الأخطاء

---

## 📋 السيناريوهات الاختبارية

### 1. سيناريو اللعبة الأساسي

**الخطوات:**
```
1. إنشاء غرفة جديدة
2. الانتظار حتى ينضم لاعب آخر
3. بدء اللعبة
4. الإجابة على 5 أسئلة
5. التحقق من النتيجة النهائية
```

**التحقق من:**
- ✅ اللعبة تبدأ بدون تأخير
- ✅ الأسئلة تظهر بشكل صحيح
- ✅ الإجابات تُعالج بشكل صحيح
- ✅ النقاط تُحسب بشكل صحيح

**المقاييس:**
```
- وقت تحميل أول سؤال: < 1 ثانية
- وقت الانتقال بين الأسئلة: < 0.5 ثانية
- استجابة الإجابة: فوري
```

---

### 2. سيناريو الرسائل المكثيفة

**الخطوات:**
```
1. فتح غرفة بـ 5 لاعبين
2. إرسال 50 رسالة متتالية بسرعة
3. التحقق من ظهور جميع الرسائل
4. قياس تأثر الأداء
```

**التحقق من:**
- ✅ جميع الرسائل تظهر في النهاية
- ✅ لا يوجد فقدان للرسائل
- ✅ واجهة الأسئلة تبقى سلسة

**المقاييس:**
```
قبل التحسين:
- 50 إشعار
- 50 rebuild للـ chat widget
- تأثر واجهة الأسئلة: نعم

بعد التحسين (batching 500ms):
- ~3-5 إشعارات
- 3-5 rebuild فقط
- تأثر واجهة الأسئلة: لا
```

---

### 3. سيناريو تحديثات المشاركين

**الخطوات:**
```
1. فتح غرفة بـ 10 لاعبين
2. مراقبة قائمة المشاركين
3. جعل اللاعبين يغادرون/يعودون
4. قياس تحديثات الترتيب
```

**التحقق من:**
- ✅ قائمة المشاركين تُحدث بشكل صحيح
- ✅ الترتيب حسب النقاط صحيح
- ✅ لا تأثير على واجهة الأسئلة

**المقاييس:**
```
- وقت تحديث قائمة المشاركين: < 100ms
- تأثر واجهة الأسئلة: 0 rebuilds
```

---

### 4. سيناريو صلاحيات المضيف

**الخطوات:**
```
1. يكون المستخدم A مضيفاً
2. A يحاول: 
   - طرد لاعب (kick)
   - تجميد لاعب (freeze)
   - ترقية لاعب (promote)
   - حذف الغرفة (delete)
3. التحقق من نجاح كل عملية
```

**التحقق من:**
- ✅ طرد اللاعب يعمل
- ✅ تجميد اللاعب يعمل
- ✅ الترقية تعمل
- ✅ حذف الغرفة يعمل
- ✅ اللاعبون المتأثرون يُخطرون

---

### 5. سيناريو الأداء تحت الحمل

**الخطوات:**
```
1. فتح غرفة بـ 20 لاعباً
2. لكل لاعب:
   - إرسال 2 رسالة/ثانية
   - الإجابة على الأسئلة
   - تحديث الحالة
3. تشغيل لمدة 5 دقائق
4. قياس الأداء
```

**المقاييس:**
```
FPS (Frames Per Second):
- قبل: 50-55 (بطيء)
- بعد: 55-60 (سلس)

Memory (MB):
- قبل: 120-150 MB
- بعد: 100-120 MB

CPU Usage:
- قبل: 40-50%
- بعد: 20-30%
```

---

## ✅ قائمة الاختبار

### الوظائف الأساسية

- [ ] إنشاء غرفة جديدة
- [ ] الانضمام إلى غرفة
- [ ] الإجابة على الأسئلة
- [ ] حساب النقاط
- [ ] عرض النتيجة النهائية
- [ ] المغادرة من الغرفة

### الرسائل والتواصل

- [ ] إرسال رسالة واحدة
- [ ] إرسال رسائل متعددة
- [ ] تحديث قائمة الرسائل
- [ ] عرض أسماء المستخدمين
- [ ] معالجة الأخطاء في الرسائل

### قائمة المشاركين

- [ ] عرض جميع المشاركين
- [ ] ترتيب المشاركين حسب النقاط
- [ ] تحديث حالة جاهزية اللاعب
- [ ] معالجة إضافة/إزالة لاعب

### صلاحيات المضيف

- [ ] المضيف يستطيع طرد لاعب
- [ ] المضيف يستطيع تجميد لاعب
- [ ] المضيف يستطيع ترقية لاعب
- [ ] المضيف يستطيع حذف الغرفة
- [ ] اللاعب المطرود يُخطر فوراً

### الأداء

- [ ] عدم وجود تسريب ذاكرة
- [ ] FPS ثابت ≥ 55
- [ ] استجابة سريعة للمدخلات
- [ ] لا يوجد تجميد/lag ملحوظ

---

## 🔄 عملية الاختبار

### المرحلة 1: الاختبار الوحدوي (Unit Testing)

```dart
// Test each provider individually

test('PuzzleStateProvider - setNextPuzzle', () {
  final provider = PuzzleStateProvider();
  
  provider.setNextPuzzle(
    puzzle: {'id': '1', 'question': 'Q1'},
    puzzleIndex: 0,
  );
  
  expect(provider.currentPuzzleIndex, equals(0));
  expect(provider.currentPuzzle != null, isTrue);
});

test('ChatProvider - message batching', () async {
  final provider = ChatProvider();
  
  // Add 10 messages rapidly
  for (int i = 0; i < 10; i++) {
    provider.addMessage({'id': i, 'text': 'Message $i'});
  }
  
  // Should batch and notify only once
  expect(provider.messageCount, equals(0)); // Still batching
  
  await Future.delayed(Duration(milliseconds: 600));
  
  expect(provider.messageCount, equals(10)); // Flushed after 500ms
});

test('ParticipantsProvider - sorting by score', () {
  final provider = ParticipantsProvider();
  
  provider.updateParticipants([
    {'userId': '1', 'username': 'A', 'score': 10},
    {'userId': '2', 'username': 'B', 'score': 20},
    {'userId': '3', 'username': 'C', 'score': 15},
  ], 'host');
  
  expect(provider.participants[0]['username'], equals('B')); // Highest score
  expect(provider.participants[1]['username'], equals('C'));
  expect(provider.participants[2]['username'], equals('A'));
});
```

### المرحلة 2: اختبار التكامل (Integration Testing)

```dart
// Test multiple providers together

testWidgets('Game flow with optimized providers', (tester) async {
  await tester.pumpWidget(testApp);
  
  // Create room
  await tester.tap(find.byIcon(Icons.add));
  await tester.pumpAndSettle();
  
  // Wait for room creation
  expect(find.byType(RoomGameView), findsOneWidget);
  
  // Submit answer
  await tester.tap(find.byText('إرسال الإجابة'));
  await tester.pumpAndSettle();
  
  // Verify score updated
  expect(find.textContaining('النقاط:'), findsOneWidget);
});
```

### المرحلة 3: اختبار الأداء (Performance Testing)

```dart
// Measure rebuild counts and frame rates

void measureRebuildCount() {
  int rebuildCount = 0;
  
  MyApp(
    onRebuild: () => rebuildCount++,
  );
  
  // Simulate 100 message events
  for (int i = 0; i < 100; i++) {
    chatProvider.addMessage({'text': 'msg $i'});
  }
  
  // Before optimization: 100 rebuilds
  // After optimization: ~2-5 rebuilds (500ms batching)
  expect(rebuildCount, lessThan(10));
}
```

---

## 📊 معايير النجاح

| المعيار | الحد الأدنى | المستهدف |
|--------|-----------|---------|
| Widget Rebuilds/min | < 50 | < 30 |
| Provider Notifications | < 20 | < 10 |
| Memory Usage | < 120 MB | < 100 MB |
| FPS | ≥ 50 | ≥ 55 |
| Frame Drops | < 2 | 0 |
| Message Latency | < 1s | < 500ms |

---

## 🐛 اختبار معالجة الأخطاء

```
1. قطع الاتصال أثناء اللعبة
   ✅ يجب إعادة الاتصال تلقائياً
   ✅ لا يجب فقدان أي بيانات

2. رسالة خادم غير صالحة
   ✅ يجب تجاهلها بدون crash
   ✅ يجب تسجيل الخطأ

3. انتهاء صلاحية الجلسة
   ✅ يجب طلب إعادة تسجيل دخول
   ✅ يجب الخروج من الغرفة

4. فشل إرسال الإجابة
   ✅ يجب إعادة المحاولة
   ✅ يجب إظهار رسالة خطأ
```

---

## 🚀 خطة النشر

1. **اختبار محلي:** 24 ساعة
2. **اختبار Beta:** أسبوع واحد (10 مستخدمين)
3. **نشر تدريجي:** 50% من المستخدمين
4. **نشر كامل:** 100% من المستخدمين

---

## 📝 ملاحظات مهمة

- تأكد من تشغيل profiler أثناء الاختبار
- اختبر على أجهزة مختلفة (هاتف قديم وحديث)
- اختبر مع اتصالات إنترنت مختلفة (4G, WiFi, بطيء)
- سجل أي مشاكل على GitHub
- احتفظ بالبيانات لمقارنة الأداء

---

**تاريخ البدء:** 2024
**الحالة:** جاهز للاختبار
